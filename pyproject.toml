[project]
name = "youle-mas"
version = "0.1.0"
description = "Unified Youle multi-agent platform workspace"
requires-python = ">=3.12"
dependencies = []

[tool.uv]
package = false

[tool.uv.workspace]
members = ["backend", "agents", "agents/mcp_servers"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
pythonpath = ["backend", "agents"]
