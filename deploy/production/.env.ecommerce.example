# Copy this to a private environment file. Never commit credentials.
# The core script intentionally remains a zero-cost mock profile.
LITELLM_MOCK=false
DEEPSEEK_API_KEY=
ARK_API_KEY=
ECOMMERCE_SEEDREAM_MODEL=doubao-seedream-4-0-250828

# A paid image smoke test is disabled by default. It still pauses for every
# image_generation_confirmation; no script may approve that gate automatically.
RUN_LIVE_ECOMMERCE_SMOKE=false
