apiVersion: apps/v1
kind: Deployment
metadata:
  name: agent-document
  namespace: haole
  labels: { app: agent-document, agent: document }
spec:
  replicas: 1
  selector: { matchLabels: { app: agent-document } }
  template:
    metadata:
      labels: { app: agent-document, agent: document }
    spec:
      serviceAccountName: haole-app
      containers:
        - name: agent
          image: ghcr.io/your-org/haole-agent:0.1.0
          env:
            - { name: AGENT_KIND, value: document }
          envFrom:
            - configMapRef: { name: haole-config }
            - secretRef: { name: haole-secrets }
          resources:
            requests: { cpu: "200m", memory: "512Mi" }
            limits:   { cpu: "1500m", memory: "1Gi" }
      terminationGracePeriodSeconds: 60
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata: { name: agent-document-hpa, namespace: haole }
spec:
  scaleTargetRef: { apiVersion: apps/v1, kind: Deployment, name: agent-document }
  minReplicas: 1
  maxReplicas: 4
  metrics:
    - type: Resource
      resource: { name: cpu, target: { type: Utilization, averageUtilization: 70 } }
