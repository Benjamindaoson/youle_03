# V1 工程基建清单

**版本**:v3.0(对齐 ADR-001-rev / 008→014 / 009-016)
**日期**:2026-05-05
**面向**:后端 / SRE / DevOps / 架构
**地位**:V1 上线前必须落地的工程基建。每条都是阻塞项,补完才能正式编码。

**v3.0 关键变更**:
- 🔄 Agent 编号反向(ADR-001-rev):队列名映射调整
- 🔄 数据库 schema:`conversations` 加 `work_mode` 列、删 `parent_conversation_id`、drop `conversation_links` 表
- 🔄 新增 `agent_status` 表(ADR-015 拟人化)
- 🔄 新增 `mode_switch_log` 表(ADR-014 模式切换审计)
- 保留:Qdrant / MCP Server / HITL / Reflexion / 飞轮信号

---

## 总览

| 模块 | 选型 | 优先级 |
|-----|------|-------|
| 1. 认证 / 授权 | JWT + 手机验证码,FastAPI Users | P0 |
| 2. WebSocket 网关 | FastAPI WS + Redis Pub/Sub | P0 |
| 3. 数据库 | PostgreSQL 16 + pgvector | P0 |
| 4. 完整 schema | **18 张表 DDL**(v2.0)| P0 |
| 5. 对象存储 | 阿里云 OSS(主) / S3(备)| P0 |
| 6. 缓存 / 队列 | Redis 7 + Redis Streams | P0 |
| 7. 模型路由 | LiteLLM Proxy | P0 |
| **8. MCP Server 集群**(v2.0)| 7 个独立服务,Python/TS | **P0** |
| **9. Qdrant 向量库**(v2.0)| 数据飞轮 RAG | **P0** |
| 10. 长任务 | Celery + Redis broker | P0 |
| 11. 配额限流 | Redis token bucket | P0 |
| 12. 配置 / Secrets | K8s Secret + 环境变量 | P0 |
| 13. 部署 | Docker Compose(dev) + K8s(prod)| P1 |
| 14. 监控 | Prometheus + Grafana + Loki | P1 |
| 15. CI / CD | GitHub Actions + ArgoCD | P1 |
| 16. 数据库迁移 | Alembic | P1 |
| 17. 测试 | pytest + httpx + locust + **MCP integration** | P1 |
| 18. 客户端 | Next.js 15 PWA + **HITL 审核组件 3 个** | P0 |
| 19. 错误码体系 | 统一码表 | P1 |
| **20. Reflexion + Skill Drafter pipeline**(v2.0)| Celery 异步任务 | P1 |

---

## 1. 认证 / 授权

### 1.1 选型

- **JWT** Access Token(15 分钟) + Refresh Token(30 天)
- **手机号 + 短信验证码**为主流注册方式
- 工具:**FastAPI Users 12.x** 集成 SQLAlchemy + JWT

### 1.2 流程

```
[注册 / 登录]
用户输入手机号 → 服务端发短信(阿里云短信)→ 用户输入验证码
→ 服务端校验 → 生成 access_token + refresh_token
→ 返回客户端 → 存 localStorage(refresh)+ 内存(access)

[API 鉴权]
客户端每次请求带 Authorization: Bearer <access_token>
中间件校验 → 注入 current_user 到 request.state

[Token 刷新]
access_token 过期 → 客户端用 refresh_token 调 /api/auth/refresh
→ 新 access_token + 新 refresh_token(rotation)

[登出]
清除客户端 token + 服务端把 refresh_token 加入 Redis 黑名单(直到自然过期)

[WebSocket 鉴权]
WS 连接时 query 参数带 token,握手阶段校验
```

### 1.3 中间件实现

```python
# app/middleware/auth.py
from fastapi import Depends, HTTPException
from fastapi_users.authentication import JWTStrategy
from app.models.user import User

async def get_current_user(
    request: Request,
    jwt_strategy: JWTStrategy = Depends(),
) -> User:
    auth_header = request.headers.get("Authorization", "")
    if not auth_header.startswith("Bearer "):
        raise HTTPException(401, "Missing token")
    
    token = auth_header[7:]
    user = await jwt_strategy.read_token(token, user_manager)
    
    if not user or not user.is_active:
        raise HTTPException(401, "Invalid or inactive user")
    
    request.state.user = user
    return user

# 用法
@router.get("/api/conversations")
async def list_conversations(user: User = Depends(get_current_user)):
    return await ConversationService.list_for_user(user.id)
```

### 1.4 API 端点

```
POST /api/auth/sms/send       发短信验证码
POST /api/auth/register       注册(手机号 + 验证码)
POST /api/auth/login          登录(手机号 + 验证码)
POST /api/auth/refresh        刷新 access_token
POST /api/auth/logout         登出
GET  /api/auth/me             获取当前用户

POST /api/auth/oauth/wechat   微信 OAuth(V1.5)
```

### 1.5 安全要点

- 短信验证码:6 位数字,5 分钟有效,Redis 存,同手机号 1 分钟内只能发 1 次
- access_token 在 Redis 不存(stateless),refresh_token 存 Redis(便于撤销)
- 密码字段不存(本产品不用密码登录)
- 敏感操作(改密码、删账号)需 step-up:再发一次短信验证码

---

## 2. WebSocket 网关

### 2.1 选型

- **FastAPI 原生 WebSocket** + **Redis Pub/Sub** 多实例 fanout
- 不引入 Socket.io(过重)

### 2.2 架构

```
浏览器                     后端 1                后端 2
   │                         │                     │
   ├──WS handshake──────────>│                     │
   │  (含 access_token)      │                     │
   │                         │                     │
   │<─────WS message────────│                     │
   │   (实时事件)            │                     │
   │                         │                     │
                             ↑                     ↑
                             │                     │
                             └─── Redis Pub/Sub ───┘
                                  channel: user:{user_id}

后端任意实例发布消息到 Redis channel
→ 持有该用户连接的实例订阅到 → 通过 WS 推给浏览器
```

### 2.3 实现

```python
# app/ws/connection.py
import asyncio
import json
from fastapi import WebSocket, WebSocketDisconnect, Depends
from redis.asyncio import Redis

class ConnectionManager:
    def __init__(self):
        self.local_connections: dict[str, WebSocket] = {}
    
    async def connect(self, ws: WebSocket, user_id: str, redis: Redis):
        await ws.accept()
        self.local_connections[user_id] = ws
        
        # 订阅 Redis pubsub
        pubsub = redis.pubsub()
        await pubsub.subscribe(f"user:{user_id}")
        
        # 启动消费协程
        consumer = asyncio.create_task(self._consume(ws, pubsub))
        
        try:
            while True:
                # 客户端发来的消息(暂时只处理 ping)
                data = await ws.receive_text()
                if data == "ping":
                    await ws.send_text("pong")
        except WebSocketDisconnect:
            consumer.cancel()
            await pubsub.unsubscribe(f"user:{user_id}")
            del self.local_connections[user_id]
    
    async def _consume(self, ws: WebSocket, pubsub):
        async for msg in pubsub.listen():
            if msg["type"] == "message":
                await ws.send_text(msg["data"])

manager = ConnectionManager()


# WS 端点
@app.websocket("/ws")
async def websocket_endpoint(
    ws: WebSocket,
    token: str,                          # query param
    redis: Redis = Depends(get_redis),
):
    user = await verify_token(token)
    if not user:
        await ws.close(code=4001)
        return
    await manager.connect(ws, user.id, redis)


# 任意位置发送事件
async def emit_to_user(user_id: str, event: dict, redis: Redis):
    await redis.publish(f"user:{user_id}", json.dumps(event))
```

### 2.4 事件协议

```typescript
type WSEvent =
  | { type: 'conversation_created'; conversation: Conversation }
  | { type: 'conversation_status_changed'; conversation_id: string; status: string }
  | { type: 'message_added'; message: Message }
  | { type: 'step_started'; task_id: string; step_id: string; agent_id: string }
  | { type: 'step_completed'; task_id: string; step_id: string; artifact: ArtifactPreview }
  | { type: 'step_streaming'; task_id: string; step_id: string; chunk: string }
  | { type: 'task_completed'; task_id: string; primary_artifact: Artifact }
  | { type: 'task_failed'; task_id: string; error: ErrorDetail }
  | { type: 'clarification_required'; task_id: string; clarification: Clarification }
  | { type: 'mode_choice_required'; conversation_id: string; options: ModeOption[] }
  | { type: 'work_group_proposed'; discuss_id: string; skill_id: string }
  | { type: 'brief_updated'; pool_id: string; brief: Brief }
  | { type: 'quota_warning'; quota_type: string; remaining: number }
  | { type: 'pong' };
```

### 2.5 重连与缓冲

- 客户端断线后 5 秒自动重连
- 服务端对每个 user 维护 ringbuffer(最近 100 条事件,Redis Stream)
- 重连时客户端带 `last_event_id` query 参数,服务端把缺失事件补发

```
GET /ws?token=xxx&last_event_id=1715000000-0
```

---

## 3. 数据库 — PostgreSQL 16 + pgvector

### 3.1 部署

- **生产**:阿里云 RDS PostgreSQL 16(主从 + 异地备份)
- **dev**:Docker `pgvector/pgvector:pg16`
- **扩展**:`pgvector` v0.7.0(语义检索)、`pg_trgm`(模糊匹配)、`pg_jieba`(中文分词,可选)

### 3.2 性能预估

V1 单库可承载 10 万 DAU。表大小预估:

| 表 | 单行 | 1 万用户 1 月数据量 |
|----|------|------------------|
| messages | 0.5 KB | 1500 万行 / 7.5 GB |
| artifacts | 1 KB | 50 万行 / 0.5 GB |
| conversations | 0.3 KB | 30 万行 / 0.1 GB |
| tasks | 1 KB | 100 万行 / 1 GB |

V2 拆库:messages 单独走 ScyllaDB / 时序库。

---

## 4. 完整 Schema(13 张表)

```sql
-- ============================================================
-- 用户与权限
-- ============================================================
CREATE TABLE users (
    id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    phone           VARCHAR(20) UNIQUE NOT NULL,
    nickname        VARCHAR(50),
    avatar_url      TEXT,
    avatar_style    VARCHAR(50),         -- 我的世界/NFT/上传
    plan            VARCHAR(20) DEFAULT 'free',  -- free / personal / team
    is_active       BOOLEAN DEFAULT TRUE,
    created_at      TIMESTAMPTZ DEFAULT NOW(),
    updated_at      TIMESTAMPTZ DEFAULT NOW(),
    last_login_at   TIMESTAMPTZ
);
CREATE INDEX idx_users_phone ON users(phone);


CREATE TABLE user_preferences (
    user_id         UUID PRIMARY KEY REFERENCES users(id) ON DELETE CASCADE,
    preferences     JSONB NOT NULL DEFAULT '{}'::jsonb,
    -- {
    --   "default_mode": {"value":"work", "confidence":1.0, "consecutive_count":3, "last_updated":"..."},
    --   "short_video_audience": {...},
    --   ...
    -- }
    updated_at      TIMESTAMPTZ DEFAULT NOW()
);


-- ============================================================
-- 会话与上下文
-- ============================================================
CREATE TABLE context_pools (
    id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id         UUID NOT NULL REFERENCES users(id),
    intent          JSONB,
    brief           JSONB DEFAULT '{"完成度":0.0,"字段":{},"决策日志":[]}'::jsonb,
    reference_artifact_ids UUID[] DEFAULT ARRAY[]::UUID[],
    user_preferences_snapshot JSONB,
    metadata        JSONB DEFAULT '{}'::jsonb,
    created_at      TIMESTAMPTZ DEFAULT NOW(),
    updated_at      TIMESTAMPTZ DEFAULT NOW()
);
CREATE INDEX idx_pool_user ON context_pools(user_id);


CREATE TABLE conversations (
    id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id         UUID NOT NULL REFERENCES users(id),
    name            VARCHAR(100) NOT NULL,
    mode            VARCHAR(20) NOT NULL CHECK (mode IN ('main_session','group','private_chat')),
    work_mode       VARCHAR(10) CHECK (work_mode IN ('plan','ask','auto')),  -- v3.0 ADR-014
    avatar_style    VARCHAR(50),
    context_pool_id UUID REFERENCES context_pools(id),
    skill_id        UUID,                                  -- FK 在 skills 表创建后加
    -- v3.0 ADR-014:删除 parent_conversation_id(不再有讨论群衍生工作群)
    private_chat_agent_id VARCHAR(20),                     -- agent_1/2/3/4(私聊时用)
    status          VARCHAR(20) DEFAULT 'active' CHECK (status IN ('active','paused','archived','deleted')),
    last_message_at TIMESTAMPTZ,
    created_at      TIMESTAMPTZ DEFAULT NOW(),
    updated_at      TIMESTAMPTZ DEFAULT NOW()
);
CREATE INDEX idx_conv_user_mode ON conversations(user_id, mode);
CREATE INDEX idx_conv_user_work_mode ON conversations(user_id, work_mode) WHERE mode='group';  -- v3.0
CREATE INDEX idx_conv_context_pool ON conversations(context_pool_id);
CREATE INDEX idx_conv_last_msg ON conversations(user_id, last_message_at DESC);


-- v3.0 ADR-014:`conversation_links` 表已废弃(不再有讨论群衍生工作群关系)
-- 改为 mode_switch_log 审计模式切换:
CREATE TABLE mode_switch_log (
    id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    conversation_id UUID NOT NULL REFERENCES conversations(id),
    from_mode       VARCHAR(10),
    to_mode         VARCHAR(10),
    triggered_by    VARCHAR(20),       -- user_explicit / orchestrator_auto / brief_threshold
    triggered_at    TIMESTAMPTZ DEFAULT NOW()
);
CREATE INDEX idx_mode_switch_conv ON mode_switch_log(conversation_id, triggered_at DESC);


-- ============================================================
-- 消息
-- ============================================================
CREATE TABLE messages (
    id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    conversation_id UUID NOT NULL REFERENCES conversations(id),
    role            VARCHAR(20) NOT NULL,
    -- user / assistant / agent_1 / agent_2 / agent_3 / agent_4 / system
    content         TEXT,
    content_type    VARCHAR(20) DEFAULT 'text',
    -- text / clarification / artifact_card / system / streaming
    metadata        JSONB DEFAULT '{}'::jsonb,
    created_at      TIMESTAMPTZ DEFAULT NOW()
);
CREATE INDEX idx_msg_conv_created ON messages(conversation_id, created_at);


-- ============================================================
-- Skill 定义与索引
-- ============================================================
CREATE TABLE skills (
    id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    skill_id        VARCHAR(100) NOT NULL UNIQUE,         -- short_video
    name            VARCHAR(100) NOT NULL,
    description     TEXT,
    domain          VARCHAR(20),                          -- text/image/video/document/mixed
    scenario        VARCHAR(50),                          -- short_video / ecommerce_detail / ...
    version         VARCHAR(20) NOT NULL,
    creator_type    VARCHAR(20) DEFAULT 'platform',       -- platform / user
    creator_id      UUID,
    visibility      VARCHAR(20) DEFAULT 'public',         -- public / subscribed / private
    keywords        TEXT[] DEFAULT ARRAY[]::TEXT[],
    anti_signals    TEXT[] DEFAULT ARRAY[]::TEXT[],
    yaml_content    TEXT NOT NULL,                        -- 完整 YAML
    inputs_schema   JSONB,                                -- 解析后字段定义
    workflow_steps  JSONB,                                -- 解析后步骤
    status          VARCHAR(20) DEFAULT 'published',      -- draft / published / deprecated
    created_at      TIMESTAMPTZ DEFAULT NOW(),
    updated_at      TIMESTAMPTZ DEFAULT NOW()
);
CREATE INDEX idx_skills_keywords ON skills USING GIN(keywords);
CREATE INDEX idx_skills_anti_signals ON skills USING GIN(anti_signals);
CREATE INDEX idx_skills_scenario ON skills(scenario);


CREATE TABLE skill_embeddings (
    skill_id        UUID PRIMARY KEY REFERENCES skills(id),
    description_vec VECTOR(1024) NOT NULL                 -- BGE-M3 1024 维
);
CREATE INDEX idx_skill_emb_hnsw ON skill_embeddings USING HNSW(description_vec vector_cosine_ops);


CREATE TABLE user_skill_visibility (
    user_id         UUID NOT NULL REFERENCES users(id),
    skill_id        UUID NOT NULL REFERENCES skills(id),
    relationship    VARCHAR(20) NOT NULL,                 -- owned / subscribed / platform
    created_at      TIMESTAMPTZ DEFAULT NOW(),
    PRIMARY KEY (user_id, skill_id)
);
CREATE INDEX idx_usv_user ON user_skill_visibility(user_id);


-- ============================================================
-- 任务 / 工作流执行
-- ============================================================
CREATE TABLE tasks (
    id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id         UUID NOT NULL REFERENCES users(id),
    conversation_id UUID NOT NULL REFERENCES conversations(id),
    skill_id        UUID REFERENCES skills(id),
    skill_version   VARCHAR(20),
    status          VARCHAR(20) DEFAULT 'pending',
    -- pending / clarifying / executing / paused / completed / cancelled / failed
    collected_fields JSONB DEFAULT '{}'::jsonb,
    progress        JSONB DEFAULT '{"current":0,"total":0}'::jsonb,
    estimated_duration_seconds INTEGER,
    started_at      TIMESTAMPTZ,
    completed_at    TIMESTAMPTZ,
    cancelled_at    TIMESTAMPTZ,
    error_detail    JSONB,
    created_at      TIMESTAMPTZ DEFAULT NOW(),
    updated_at      TIMESTAMPTZ DEFAULT NOW()
);
CREATE INDEX idx_tasks_user_status ON tasks(user_id, status);
CREATE INDEX idx_tasks_conv ON tasks(conversation_id);


CREATE TABLE task_steps (
    id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    task_id         UUID NOT NULL REFERENCES tasks(id) ON DELETE CASCADE,
    step_id         VARCHAR(50) NOT NULL,                 -- skill workflow 里的 step_id
    agent_id        VARCHAR(20),
    task_type       VARCHAR(50),
    status          VARCHAR(20) DEFAULT 'pending',
    -- pending / running / completed / failed / skipped / pending_external
    started_at      TIMESTAMPTZ,
    completed_at    TIMESTAMPTZ,
    duration_ms     INTEGER,
    cost_usd        DECIMAL(10,6),
    model_used      VARCHAR(100),
    output_artifact_id UUID,
    error_detail    JSONB,
    metadata        JSONB DEFAULT '{}'::jsonb,
    UNIQUE(task_id, step_id)
);
CREATE INDEX idx_step_task ON task_steps(task_id);


-- ============================================================
-- 产物
-- ============================================================
CREATE TABLE artifacts (
    id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id         UUID NOT NULL REFERENCES users(id),
    context_pool_id UUID REFERENCES context_pools(id),
    source_conversation_id UUID NOT NULL REFERENCES conversations(id),
    source_task_id  UUID REFERENCES tasks(id),
    source_step_id  VARCHAR(50),
    type            VARCHAR(30) NOT NULL,
    -- text / image / image_collection / video / audio / document / structured / quality_report
    is_reference    BOOLEAN DEFAULT FALSE,
    is_final        BOOLEAN DEFAULT FALSE,
    reference       TEXT NOT NULL,                        -- OSS URL
    metadata        JSONB DEFAULT '{}'::jsonb,
    created_at      TIMESTAMPTZ DEFAULT NOW()
);
CREATE INDEX idx_artifact_user ON artifacts(user_id);
CREATE INDEX idx_artifact_pool ON artifacts(context_pool_id);
CREATE INDEX idx_artifact_source ON artifacts(source_conversation_id);
CREATE INDEX idx_artifact_task ON artifacts(source_task_id);


-- ============================================================
-- 配额追踪
-- ============================================================
CREATE TABLE quota_usage (
    id              BIGSERIAL PRIMARY KEY,
    user_id         UUID NOT NULL REFERENCES users(id),
    quota_type      VARCHAR(30) NOT NULL,
    -- agent_call_daily / video_task_daily / work_group_monthly / discuss_group_monthly / token_total
    period          VARCHAR(20) NOT NULL,                 -- 2026-05-05 / 2026-05 / 2026
    consumed        BIGINT DEFAULT 0,
    last_used_at    TIMESTAMPTZ DEFAULT NOW(),
    UNIQUE(user_id, quota_type, period)
);
CREATE INDEX idx_quota_lookup ON quota_usage(user_id, quota_type, period);


-- ============================================================
-- BGM 素材库
-- ============================================================
CREATE TABLE bgm_library (
    id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    title           VARCHAR(100),
    mood            VARCHAR(30) NOT NULL,
    -- warning / sad / hopeful / urgent / cheerful / neutral / serious
    duration        INTEGER NOT NULL,                     -- 秒
    bpm             INTEGER,
    oss_ref         TEXT NOT NULL,
    license         VARCHAR(50),                          -- 商用授权信息
    usage_count     BIGINT DEFAULT 0,
    is_active       BOOLEAN DEFAULT TRUE,
    created_at      TIMESTAMPTZ DEFAULT NOW()
);
CREATE INDEX idx_bgm_mood_duration ON bgm_library(mood, duration) WHERE is_active = TRUE;


-- ============================================================
-- 反向 FK 补充
-- ============================================================
ALTER TABLE conversations ADD CONSTRAINT fk_conv_skill 
    FOREIGN KEY (skill_id) REFERENCES skills(id);
```

### 4.0.4 v3.0 conversations 表迁移(ADR-014)

```sql
-- v3 迁移:三模式同群切换替代两种独立群
ALTER TABLE conversations
    ADD COLUMN work_mode VARCHAR(10) CHECK (work_mode IN ('plan','ask','auto'));

UPDATE conversations SET mode='group', work_mode='plan' WHERE mode='discuss';
UPDATE conversations SET mode='group', work_mode='auto' WHERE mode='work';
UPDATE conversations SET work_mode='plan' WHERE mode='main_session';

ALTER TABLE conversations 
    DROP COLUMN parent_conversation_id,
    DROP CONSTRAINT conversations_mode_check,
    ADD CONSTRAINT conversations_mode_check 
        CHECK (mode IN ('main_session','group','private_chat'));

DROP TABLE conversation_links;

-- 新增模式切换审计表
CREATE TABLE mode_switch_log (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    conversation_id UUID NOT NULL REFERENCES conversations(id),
    from_mode VARCHAR(10),
    to_mode VARCHAR(10),
    triggered_by VARCHAR(20),
    triggered_at TIMESTAMPTZ DEFAULT NOW()
);
CREATE INDEX idx_mode_switch_conv ON mode_switch_log(conversation_id);
```

### 4.0.5 v3.0 新增表 — Agent 拟人化(ADR-015)

```sql
-- Agent 工作状态
CREATE TABLE agent_status (
    user_id UUID NOT NULL REFERENCES users(id),
    agent_id VARCHAR(20) NOT NULL,         -- agent_1 ~ agent_4 / hr / finance_manager
    status VARCHAR(20) NOT NULL,           -- working / idle / fishing / training
    last_active_at TIMESTAMPTZ DEFAULT NOW(),
    PRIMARY KEY (user_id, agent_id)
);

-- 表情触发记录(可选,V1.5)
CREATE TABLE emotion_signals (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id UUID NOT NULL REFERENCES users(id),
    conversation_id UUID NOT NULL REFERENCES conversations(id),
    agent_id VARCHAR(20) NOT NULL,
    emotion VARCHAR(30),                    -- happy / proud / thinking / frustrated / sad / cool
    triggered_by VARCHAR(50),               -- task_completed / failed / praised / criticized
    created_at TIMESTAMPTZ DEFAULT NOW()
);
CREATE INDEX idx_emotion_user ON emotion_signals(user_id, created_at DESC);
```

### 4.0.6 v2.0 已有 5 张表(对应 ADR-010 / ADR-011)

```sql
-- HITL gate 历史(ADR-010)
CREATE TABLE hitl_gates (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    task_id UUID NOT NULL REFERENCES tasks(id) ON DELETE CASCADE,
    step_id VARCHAR(50) NOT NULL,
    gate_type VARCHAR(30) NOT NULL,             -- version_select / quality_review / final_approval
    opened_at TIMESTAMPTZ DEFAULT NOW(),
    closed_at TIMESTAMPTZ,
    timeout_seconds INTEGER DEFAULT 600,
    resolution VARCHAR(20),                     -- approved / modified / rolled_back / timeout
    user_choice JSONB,
    preview_artifact_id UUID REFERENCES artifacts(id),
    metadata JSONB
);
CREATE INDEX idx_hitl_task ON hitl_gates(task_id);
CREATE INDEX idx_hitl_open ON hitl_gates(task_id) WHERE closed_at IS NULL;

-- 工作流轨迹(数据飞轮信号 1)
CREATE TABLE workflow_traces (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    task_id UUID UNIQUE REFERENCES tasks(id),
    user_id UUID REFERENCES users(id),
    skill_id UUID REFERENCES skills(id),
    skill_version VARCHAR(20),
    duration_ms INTEGER,
    cost_usd DECIMAL(10,6),
    user_satisfaction SMALLINT,                 -- 1-5
    failure_reason TEXT,
    trace_oss_ref TEXT,
    qdrant_point_id TEXT,
    rollback_count SMALLINT DEFAULT 0,
    created_at TIMESTAMPTZ DEFAULT NOW()
);
CREATE INDEX idx_trace_user_skill ON workflow_traces(user_id, skill_id);
CREATE INDEX idx_trace_satisfaction ON workflow_traces(user_satisfaction) WHERE user_satisfaction IS NOT NULL;

-- Prompt 改进候选(数据飞轮信号 3,Reflexion)
CREATE TABLE prompt_improvement_candidates (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    prompt_name VARCHAR(100) NOT NULL,          -- prompts.py 里的常量名
    failure_task_id UUID REFERENCES tasks(id),
    root_cause TEXT,
    section_to_improve TEXT,
    current_text TEXT,
    proposed_changes JSONB,                     -- 含多个备选 + 利弊
    expected_improvement TEXT,
    status VARCHAR(20) DEFAULT 'pending',       -- pending / approved / rejected / merged
    reviewed_by UUID REFERENCES users(id),
    reviewed_at TIMESTAMPTZ,
    created_at TIMESTAMPTZ DEFAULT NOW()
);
CREATE INDEX idx_pic_pending ON prompt_improvement_candidates(prompt_name) WHERE status = 'pending';

-- Skill 草稿(数据飞轮信号 4,创作者飞轮 V1.5)
CREATE TABLE skill_drafts (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id UUID REFERENCES users(id),
    source_task_id UUID REFERENCES tasks(id),
    draft_yaml TEXT NOT NULL,
    name VARCHAR(100),
    description TEXT,
    status VARCHAR(20) DEFAULT 'draft',         -- draft / submitted / approved / rejected / published
    submitted_at TIMESTAMPTZ,
    reviewed_at TIMESTAMPTZ,
    review_notes TEXT,
    published_skill_id UUID REFERENCES skills(id),
    created_at TIMESTAMPTZ DEFAULT NOW()
);
CREATE INDEX idx_drafts_user ON skill_drafts(user_id, status);

-- user_preferences 表加列(信号 2)
ALTER TABLE user_preferences 
ADD COLUMN preference_vec VECTOR(256),          -- bge-base-zh-v1.5
ADD COLUMN total_choices INTEGER DEFAULT 0,
ADD COLUMN preference_updated_at TIMESTAMPTZ;
CREATE INDEX idx_user_pref_vec ON user_preferences USING HNSW(preference_vec vector_cosine_ops);

-- artifacts 表加列(支持回滚后旧产物保留)
ALTER TABLE artifacts
ADD COLUMN version INTEGER DEFAULT 1,
ADD COLUMN superseded_by UUID REFERENCES artifacts(id);
CREATE INDEX idx_artifact_step_version ON artifacts(source_task_id, source_step_id, version);
```

---

### 4.1 表关系图

```
users
 ├─ user_preferences (1:1)
 ├─ context_pools (1:n,v3.0:每群一个,不跨群共享)
 │   └─ conversations (1:1 with pool;mode + work_mode 决定形态)
 │       ├─ messages (1:n)
 │       ├─ tasks (1:n)
 │       │   └─ task_steps (1:n)
 │       └─ mode_switch_log (1:n,v3.0 ADR-014 审计)
 ├─ artifacts (1:n)
 ├─ quota_usage (1:n)
 ├─ agent_status (1:n,v3.0 ADR-015)
 └─ user_skill_visibility (n:n with skills)

skills
 ├─ skill_embeddings (1:1)
 └─ user_skill_visibility (n:n)

bgm_library  (独立,只读)
```

---

## 5. 对象存储

### 5.1 选型

- **生产主**:阿里云 OSS(国内访问快、价格低)
- **生产备**:AWS S3(海外用户、灾备)
- **dev**:MinIO(单机 Docker)

### 5.2 桶规划

```
oss://haole-prod/
├── samples/                    # 平台样本图(澄清形式 3 用)
├── bgm/                        # BGM 素材
├── users/{user_id}/
│   ├── uploads/                # 用户上传素材
│   └── avatars/
└── artifacts/{task_id}/
    └── {step_id}/
        └── output.{ext}        # 任务产物
```

### 5.3 上传策略:**前端直传 + 后端预签名 URL**

```
前端                       后端                       OSS
  │                          │                          │
  ├──POST /api/upload/sign──>│                          │
  │  (file_name, size)       │                          │
  │                          │                          │
  │                          ├──GenSignedUrl───────────>│
  │                          │<──signed_put_url─────────┤
  │<─────signed_url──────────┤                          │
  │                          │                          │
  ├─────PUT signed_url(file)──────────────────────────->│
  │                                                     │
  │<──────────────────200 OK────────────────────────────┤
  │                          │                          │
  ├──POST /api/upload/confirm>│                          │
  │  (oss_path)              │                          │
  │                          ├──写入 artifacts 表        │
  │<─────artifact_id─────────┤                          │
```

### 5.4 实现

```python
# app/services/oss.py
import oss2
from datetime import timedelta

class OSSService:
    def __init__(self, settings):
        auth = oss2.Auth(settings.OSS_ACCESS_KEY, settings.OSS_SECRET_KEY)
        self.bucket = oss2.Bucket(auth, settings.OSS_ENDPOINT, settings.OSS_BUCKET)
    
    async def generate_put_url(
        self, 
        path: str,
        max_size_mb: int = 100,
        expires: timedelta = timedelta(minutes=15)
    ) -> str:
        return self.bucket.sign_url(
            "PUT",
            path,
            expires.total_seconds(),
            headers={"Content-Length": str(max_size_mb * 1024 * 1024)},
        )
    
    async def generate_get_url(
        self,
        path: str,
        expires: timedelta = timedelta(hours=1),
    ) -> str:
        return self.bucket.sign_url("GET", path, expires.total_seconds())
    
    async def upload_bytes(self, path: str, data: bytes) -> str:
        self.bucket.put_object(path, data)
        return f"oss://{self.bucket.bucket_name}/{path}"
    
    async def download_bytes(self, oss_url: str) -> bytes:
        # oss://bucket/path → path
        path = oss_url.split("/", 3)[-1]
        return self.bucket.get_object(path).read()
```

### 5.5 生命周期与冷归档

OSS 桶规则:

```
artifacts/{任意}: 
  - 90 天后转 IA 存储(便宜 50%)
  - 180 天后转归档存储(便宜 80%)

users/{user_id}/uploads/:
  - 用户主动删除即立即删除
```

### 5.6 安全

- 桶默认私有,所有访问走预签名 URL
- 签名 URL 默认 1 小时过期
- 上传前在后端校验:文件类型白名单、大小上限、用户配额
- 用户上传内容过病毒扫描(阿里云内容安全)

---

## 6. Redis 7 + Redis Streams

### 6.1 用途

| 用途 | key pattern | 数据结构 |
|------|-------------|---------|
| Agent 任务队列 | `agent_tasks:{agent_name}` | Stream |
| Agent 结果队列 | `agent_results:{task_id}_{step_id}` | Stream |
| 短信验证码 | `sms_code:{phone}` | String + TTL 5min |
| Refresh token 黑名单 | `revoked_refresh:{token_hash}` | String + TTL |
| WS 用户连接索引 | `ws_user:{user_id}` | Set(实例 ID 列表)|
| WS Pub/Sub 频道 | `user:{user_id}` | Pub/Sub |
| Skill 编译缓存 | `compiled_skill:{skill_id}:{version}` | String |
| 配额计数(原子)| `quota:{user_id}:{type}:{period}` | Counter |
| Brief 防抖计数 | `brief_pending:{pool_id}` | Counter + TTL |
| Brief 防抖触发器 | `brief_debounce:{pool_id}` | 定时任务句柄 |
| LangGraph state 缓存 | (LangGraph 自己管)| - |

### 6.2 部署

- 生产:阿里云 Redis 7 主从 + sentinel
- 内存:8 GB 起步,V1 单实例够用
- dev:Docker `redis:7-alpine`

---

## 7. 模型路由 — LiteLLM Proxy

### 7.1 部署

独立的 LiteLLM Proxy 容器,所有 Agent + 主编排都走这个 Proxy。

```yaml
# docker-compose.yml
litellm_proxy:
  image: ghcr.io/berriai/litellm:main-latest
  ports:
    - "4000:4000"
  environment:
    LITELLM_MASTER_KEY: ${LITELLM_MASTER_KEY}
    DATABASE_URL: postgresql://...    # 用于 spend tracking
  volumes:
    - ./config/litellm_config.yaml:/app/config.yaml
  command: ["--config", "/app/config.yaml", "--port", "4000"]
```

### 7.2 配置文件结构

详见 [模型路由表 §2.3](../4_附录/模型路由表.md)。两份配置:

- `litellm_config_l1.yaml`(编排层)
- `litellm_config_l2.yaml`(执行层)

通过 router 标签区分:`task_type` 决定走哪个路由组。

### 7.3 客户端代码

```python
# 所有 LLM 调用统一走 Proxy
from openai import AsyncOpenAI

client = AsyncOpenAI(
    api_key=settings.LITELLM_MASTER_KEY,
    base_url="http://litellm_proxy:4000",
)

response = await client.chat.completions.create(
    model="l1_orchestration_primary",   # 等价于走主备链
    messages=[...],
    response_format={"type": "json_object"},
)
```

### 7.4 监控

LiteLLM Proxy 自带:

- 每模型成本统计
- 错误率
- 延迟分布
- 自动 Fallback 触发次数

通过 `/metrics` 端点暴露 Prometheus 指标。

---

## 8. Celery + Redis

### 8.1 用途

- 视频合成(5-10 分钟)
- 大批量图生成
- 离线任务(brief 完成度过 0.8 后的主动建议、孤儿 pool 清理)

### 8.2 部署

```yaml
celery_worker_video:
  image: haole/agent-av:latest
  command: ["celery", "-A", "app.celery_app", "worker", "-Q", "video_tasks", "-c", "2"]
  deploy:
    replicas: 2
  environment:
    CELERY_BROKER_URL: redis://redis:6379/1
    CELERY_RESULT_BACKEND: redis://redis:6379/1
```

### 8.3 长任务回调机制

详见 [4 个分任务 Agent 实现指南 §4.6](../2_工程实现/4 个分任务 Agent 实现指南.md)。

---

## 9. 配额限流

### 9.1 配额类型

| 类型 | 周期 | 免费版 | 个人版 | 团队版 |
|------|------|--------|--------|--------|
| `agent_call_daily` | 日 | 30 | 200 | 不限 |
| `video_task_daily` | 日 | 3 | 50 | 不限 |
| `work_group_monthly` | 月 | 5 | 50 | 500 |
| `discuss_group_monthly` | 月 | 20 | 50 | 不限 |
| `token_total_monthly` | 月 | 100K | 5M | 50M |
| `reference_image_per_discuss` | 单群 | 5 | 不限 | 不限 |

### 9.2 实现 — Redis 原子计数

```python
# app/services/quota.py
from datetime import date

class QuotaService:
    async def check_and_consume(
        self, user_id: str, quota_type: str, amount: int = 1
    ) -> tuple[bool, int, int]:
        """
        Returns: (success, current_used, limit)
        """
        period = self._get_period(quota_type)            # 日 / 月
        limit = self._get_limit(user_id, quota_type)     # 按用户 plan
        
        key = f"quota:{user_id}:{quota_type}:{period}"
        
        # Redis Lua 原子操作:check + incr
        lua = """
        local current = tonumber(redis.call('GET', KEYS[1]) or '0')
        local limit = tonumber(ARGV[1])
        local amount = tonumber(ARGV[2])
        if current + amount > limit then
            return {0, current, limit}
        end
        local new_val = redis.call('INCRBY', KEYS[1], amount)
        redis.call('EXPIRE', KEYS[1], ARGV[3])
        return {1, new_val, limit}
        """
        ttl_seconds = self._get_ttl(quota_type)
        result = await redis.eval(lua, 1, key, limit, amount, ttl_seconds)
        success = bool(result[0])
        
        # 异步同步到 PostgreSQL 用于审计
        if success:
            await self._async_persist(user_id, quota_type, period, amount)
        
        return success, result[1], result[2]
    
    async def warn_if_low(self, user_id: str, quota_type: str):
        """剩余 < 20% 时通过 WS 推送警告"""
        ...
```

### 9.3 中间件

```python
# 在路由装饰器里使用
@router.post("/api/tasks/start")
async def start_task(
    payload: dict,
    user: User = Depends(get_current_user),
    quota: QuotaService = Depends(),
):
    # 视频任务额外检查
    if payload.get("scenario") == "video":
        ok, used, limit = await quota.check_and_consume(
            user.id, "video_task_daily"
        )
        if not ok:
            raise QuotaExceededException("video_task_daily", used, limit)
    
    # 通用 Agent 调用配额
    ok, used, limit = await quota.check_and_consume(
        user.id, "agent_call_daily"
    )
    if not ok:
        raise QuotaExceededException("agent_call_daily", used, limit)
    
    return await orchestrator.start_task(...)
```

### 9.4 用户面提示

详见 [系统 Prompt 全集 §7.10](../4_附录/系统 Prompt 全集.md#710-配额耗尽quota_exhausted_messages)。

---

## 10. 配置 / Secrets

### 10.1 三层配置

```
1. 默认值          → 代码内 Pydantic Settings 默认
2. 环境变量        → docker-compose .env / K8s ConfigMap
3. Secret(敏感)   → K8s Secret(Sealed Secrets)/ Vault
```

### 10.2 环境变量清单

```bash
# 数据库
DATABASE_URL=postgresql+asyncpg://...
REDIS_URL=redis://redis:6379/0

# OSS
OSS_ENDPOINT=https://oss-cn-hangzhou.aliyuncs.com
OSS_BUCKET=haole-prod
OSS_ACCESS_KEY=                # Secret
OSS_SECRET_KEY=                # Secret

# LiteLLM
LITELLM_PROXY_URL=http://litellm_proxy:4000
LITELLM_MASTER_KEY=            # Secret

# 各模型 API key(走 LiteLLM 时通过 Proxy 注入,服务无需直接持有)
DEEPSEEK_API_KEY=              # Secret(注入到 LiteLLM)
ANTHROPIC_API_KEY=             # Secret
OPENAI_API_KEY=                # Secret
KIMI_API_KEY=                  # Secret
KLING_API_KEY=                 # Secret
SEEDREAM_API_KEY=              # Secret

# 工具
TAVILY_API_KEY=                # Secret
ALIYUN_SMS_ACCESS_KEY=         # Secret
ALIYUN_SMS_SECRET_KEY=         # Secret
ALIYUN_OCR_KEY=                # Secret
VOLCENGINE_TTS_KEY=            # Secret

# 应用
JWT_SECRET=                    # Secret(64 字节随机)
CORS_ALLOWED_ORIGINS=https://app.haole.ai
LOG_LEVEL=INFO
ENV=production
```

### 10.3 Secrets 策略

- 生产用 K8s Sealed Secrets(controller 解密、Pod 透明使用)
- dev 用本地 `.env`(加入 `.gitignore`)
- CI 用 GitHub Actions Secrets

---

## 11. 部署

### 11.1 环境矩阵

| 环境 | 部署方式 | 数据库 | 备注 |
|-----|---------|--------|------|
| dev | Docker Compose | PostgreSQL 16 容器 | 单机,本地开发 |
| staging | K8s(单 namespace)| 阿里云 RDS staging | 预上线,产品验收 |
| prod | K8s(多 namespace)| 阿里云 RDS prod 主从 | 生产 |

### 11.2 Dev:docker-compose.yml(简化版)

```yaml
version: '3.9'
services:
  postgres:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_DB: haole
      POSTGRES_USER: haole
      POSTGRES_PASSWORD: dev_password
    volumes: [pg_data:/var/lib/postgresql/data]
    ports: ["5432:5432"]
  
  redis:
    image: redis:7-alpine
    ports: ["6379:6379"]
  
  minio:
    image: minio/minio:latest
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: haole
      MINIO_ROOT_PASSWORD: dev_password
    ports: ["9000:9000", "9001:9001"]
  
  litellm:
    image: ghcr.io/berriai/litellm:main-latest
    env_file: .env
    volumes: [./config/litellm.yaml:/app/config.yaml]
    ports: ["4000:4000"]
  
  backend:
    build: ./backend
    env_file: .env
    depends_on: [postgres, redis, litellm]
    ports: ["8000:8000"]
    command: uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
  
  agent_text:
    build: ./agents/text
    env_file: .env
    depends_on: [redis, litellm]
    deploy: {replicas: 2}
  
  agent_image:
    build: ./agents/image
    env_file: .env
    depends_on: [redis, litellm]
    deploy: {replicas: 2}
  
  agent_av:
    build: ./agents/av
    env_file: .env
    depends_on: [redis, litellm]
    deploy: {replicas: 1}
  
  agent_document:
    build: ./agents/document
    env_file: .env
    depends_on: [redis]
    deploy: {replicas: 1}
  
  celery_video:
    build: ./agents/av
    command: celery -A app.celery_app worker -Q video_tasks -c 2
    env_file: .env
    depends_on: [redis]
  
  frontend:
    build: ./frontend
    ports: ["3000:3000"]

volumes:
  pg_data:
```

### 11.3 生产:K8s 资源规划

| 服务 | 副本 | CPU | 内存 |
|------|-----|-----|------|
| backend | 4 | 1c | 2G |
| litellm-proxy | 2 | 0.5c | 1G |
| agent-text | 4 | 2c | 4G |
| agent-image | 4 | 4c | 8G |
| agent-av | 2 | 4c | 8G(GPU 节点)|
| agent-document | 2 | 2c | 4G |
| celery-video | 4 | 4c | 8G(GPU 节点)|
| frontend(SSR)| 2 | 1c | 2G |

V1 总资源:约 20 vCPU / 50 GB 内存 + 2 个 GPU 节点(视频处理)。

---

## 12. 监控

### 12.1 选型

- **指标**:Prometheus + Grafana
- **日志**:Loki + Promtail(轻量,比 ELK 省资源)
- **Tracing**:Jaeger(可选,V1.5)
- **Uptime**:阿里云云监控(端口探活 + URL 探活)

### 12.2 关键指标

**主编排**:

- `orchestrator_intent_understanding_duration_seconds` (P50/P95/P99)
- `orchestrator_intent_confidence_distribution`
- `orchestrator_skill_match_layer_hit{layer="L1|L2|L3"}`(三层检索哪一层命中)
- `orchestrator_clarification_rounds_per_task`
- `orchestrator_interrupt_class_distribution{class="A|B|..."}`

**Agent**:

- `agent_task_duration_seconds{agent="agent_1|2|3|4", task_type="..."}`
- `agent_task_status_total{status="success|failed|timeout|out_of_scope"}`
- `agent_queue_pending_size{queue="agent_tasks:text|image|av|document"}`
- `agent_model_cost_usd_total{model="..."}`

**业务**:

- `mode_choice_distribution{mode="discuss|work|unsure"}`
- `discuss_to_work_conversion_rate`
- `brief_completion_threshold_crossings_total`
- `work_group_back_to_discuss_rate`
- `task_completion_p95_duration_seconds{skill="..."}`

**基础设施**:

- DB 连接池、慢查询、表大小
- Redis 命中率、内存使用
- WS 活跃连接数
- OSS 上传 / 下载速率

### 12.3 关键告警规则

```yaml
# alerts.yaml
- alert: HighIntentJSONFailureRate
  expr: rate(orchestrator_intent_json_parse_failed[5m]) > 0.1
  for: 5m
  annotations:
    summary: "意图理解 JSON 解析失败率过高"
    runbook: "检查 deepseek-v4-flash 健康度,可能要降级到 Haiku"

- alert: AgentQueueBacklog
  expr: agent_queue_pending_size > 100
  for: 10m
  annotations:
    summary: "Agent 队列积压"

- alert: ModelCostSpike
  expr: rate(agent_model_cost_usd_total[1h]) > 10
  for: 30m
  annotations:
    summary: "每小时模型成本超过 $10"

- alert: WorkGroupCreationFailure
  expr: rate(work_group_creation_failed_total[5m]) > 0.05
  for: 5m
  annotations:
    summary: "工作群创建失败率超过 5%"
```

---

## 13. CI / CD

### 13.1 流水线

```
PR 提交
  ├─ Lint (ruff + mypy)
  ├─ Unit tests (pytest)
  ├─ Integration tests (containerized)
  ├─ Prompt regression tests
  └─ Docker build (push to ghcr 测试镜像)

merge 到 main
  ├─ 上面全部 +
  ├─ E2E tests (staging)
  └─ ArgoCD 自动同步到 staging

手动触发"上线"
  ├─ Image promote(staging → prod tag)
  └─ ArgoCD 同步到 prod(灰度 10% → 50% → 100%)
```

### 13.2 GitHub Actions 模板

```yaml
# .github/workflows/ci.yml
name: CI
on: [pull_request]
jobs:
  lint-test:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: pgvector/pgvector:pg16
        env: {POSTGRES_PASSWORD: test}
        ports: ["5432:5432"]
      redis:
        image: redis:7-alpine
        ports: ["6379:6379"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: {python-version: "3.12"}
      - run: pip install -r requirements-dev.txt
      - run: ruff check .
      - run: mypy app/
      - run: pytest tests/ -v --cov=app --cov-fail-under=70
      - run: pytest tests/prompts/ -v   # prompt 回归集
```

---

## 14. 数据库迁移

### 14.1 工具:Alembic

```bash
# 初始化
alembic init migrations

# 自动生成迁移
alembic revision --autogenerate -m "add_brief_column"

# 应用
alembic upgrade head

# 回滚
alembic downgrade -1
```

### 14.2 上线时机

- 数据库迁移**先于**应用部署执行
- 向后兼容(老应用能跑新 schema 至少 1 个版本)
- 大表 DDL 用 `pg_repack` 避免锁

---

## 15. 测试

### 15.1 工具

- 单元 / 集成:**pytest** + **httpx** + **pytest-asyncio**
- 负载:**locust**
- 端到端:**Playwright**(前端 + 后端联调)
- Prompt 回归:**自定义 pytest 框架**(基于 system prompt 全集的 yaml 测试用例)

### 15.2 关键测试集

| 测试集 | 覆盖范围 | 跑频率 |
|--------|---------|-------|
| 单元测试 | 函数级,核心逻辑 | 每 PR |
| 集成测试 | 服务间(主编排 ↔ Agent ↔ DB)| 每 PR |
| Prompt 回归 | 8 个 system prompt 各 10 用例 | 每 PR |
| Skill 端到端 | 短视频 / 电商详情图全链路 | 每日 |
| 负载测试 | 100 并发用户 5 分钟 | 上线前 |
| 灰度验证 | 10% 流量观察 30 分钟 | 上线 |

---

## 16. 客户端 — Next.js 15 PWA

### 16.1 选型

- **Next.js 15** App Router + RSC
- **Tailwind CSS** + **shadcn/ui**
- **Zustand** 状态管理
- **PWA**(V1 不做原生客户端)

### 16.2 关键能力

- 三栏布局 + 响应式(手机端三栏可折叠)
- WebSocket 长连(自动重连 + 缺失事件补齐)
- OSS 直传
- 离线消息缓存(用户重新连接时与服务端比对)
- 暗色 / 明亮主题

### 16.3 性能目标

- LCP < 2.5s(3G 网络)
- 首屏 JS bundle < 200 KB(gzipped)
- WS 重连延迟 < 1s

---

## 17. 错误码体系

### 17.1 统一格式

```json
{
  "error": {
    "code": "QUOTA_EXCEEDED_VIDEO_DAILY",
    "message": "你今天的视频任务配额已用完",
    "user_facing": true,
    "details": {
      "quota_type": "video_task_daily",
      "used": 3,
      "limit": 3,
      "reset_at": "2026-05-06T00:00:00+08:00"
    },
    "retry_after_seconds": 3600
  }
}
```

### 17.2 错误码命名

`{大类}_{子类}_{具体}`:

| 前缀 | 大类 |
|------|------|
| `AUTH_` | 认证 |
| `QUOTA_` | 配额 |
| `VALIDATION_` | 输入校验 |
| `SKILL_` | Skill 相关 |
| `AGENT_` | Agent 错误 |
| `MODEL_` | 模型调用 |
| `OSS_` | 对象存储 |
| `INTERNAL_` | 内部错误 |

例:

- `AUTH_TOKEN_EXPIRED`
- `QUOTA_EXCEEDED_VIDEO_DAILY`
- `VALIDATION_FIELD_MISSING`
- `SKILL_NOT_FOUND`
- `AGENT_TASK_TIMEOUT`
- `MODEL_RATE_LIMITED`
- `OSS_UPLOAD_TOO_LARGE`
- `INTERNAL_DATABASE_ERROR`

### 17.3 用户面文案映射

每个 `user_facing: true` 的错误码必须在 `4_附录/系统 Prompt 全集.md §7` 有对应文案。

---

## 7.5 MCP Server 集群(v2.0,ADR-009)

### 7.5.1 部署清单

| Server | 实现 | 镜像 | 资源 | 副本 |
|--------|------|------|------|------|
| `mcp-search` | Python | `haole/mcp-search:v1.0` | 0.5c / 1G | 2 |
| `mcp-image-tools` | Python | `haole/mcp-image-tools:v1.0` | 1c / 2G + GPU 选 | 2 |
| `mcp-video-tools` | Python | `haole/mcp-video-tools:v1.0` | 4c / 8G + GPU | 2 |
| `mcp-audio-tools` | Python | `haole/mcp-audio-tools:v1.0` | 2c / 4G | 2 |
| `mcp-document-tools` | Python | `haole/mcp-document-tools:v1.0` | 1c / 2G | 2 |
| `mcp-oss` | Python | `haole/mcp-oss:v1.0` | 0.5c / 1G | 2 |
| `mcp-platform-publish`(V1.5)| TypeScript | `haole/mcp-platform-publish:v1.5` | 0.5c / 1G | 2 |

### 7.5.2 K8s 部署示例

```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mcp-search
spec:
  replicas: 2
  selector:
    matchLabels: {app: mcp-search}
  template:
    metadata:
      labels: {app: mcp-search}
    spec:
      containers:
      - name: server
        image: haole/mcp-search:v1.0
        env:
          - name: TAVILY_API_KEY
            valueFrom: {secretKeyRef: {name: api-keys, key: tavily}}
        ports: [{containerPort: 8000}]
        livenessProbe:
          httpGet: {path: /health, port: 8000}
          initialDelaySeconds: 30
        resources:
          limits: {cpu: 500m, memory: 1Gi}
---
apiVersion: v1
kind: Service
metadata:
  name: mcp-search
spec:
  ports: [{port: 8000}]
  selector: {app: mcp-search}
```

### 7.5.3 Agent 端配置

`agents/text_agent/main.py`:

```python
async def main():
    await mcp_client.init([
        ServerConfig(name="search", transport="sse", url="http://mcp-search:8000/sse"),
        ServerConfig(name="oss", transport="sse", url="http://mcp-oss:8000/sse"),
    ])
    await consume_tasks("agent_tasks:text")
```

### 7.5.4 监控

- 每个 MCP server 暴露 `/metrics`(Prometheus)
- 关键指标:`mcp_tool_call_total{server,tool,status}` / `mcp_tool_call_duration_seconds`
- 错误率 > 5% 触发告警

详见 [MCP 集成方案](../3_决策记录/MCP 集成方案.md)。

---

## 7.6 Qdrant 向量库(v2.0,ADR-011)

### 7.6.1 部署

| 项 | 选型 |
|----|------|
| 版本 | Qdrant 1.13+ |
| 部署 | K8s StatefulSet 3 副本 |
| 存储 | SSD 100GB(初期)|
| 备份 | 每日快照到 OSS |

### 7.6.2 集合规划

| Collection | 维度 | 距离 | 用途 |
|-----------|------|------|------|
| `workflow_traces` | 1024(BGE-M3)| Cosine | 数据飞轮信号 1 |
| `skill_descriptions` | 1024 | Cosine | Skill 匹配第 2 层(语义检索)|
| `user_uploads`(V1.5)| 1024 | Cosine | 用户素材库语义搜索 |

### 7.6.3 与 pgvector 的分工

| 用途 | 选 |
|------|-----|
| Skill 匹配(高频 / 数据少) | **pgvector**(几百个 Skill,事务一致性)|
| 用户偏好向量(每用户 1 个 256 维)| **pgvector**(随用户表)|
| 工作流轨迹(高频写 / 数据多)| **Qdrant**(百万级)|

### 7.6.4 Embedding 服务

**自托管 BGE-M3**:

```yaml
# K8s deployment
- name: bge-m3-service
  image: haole/bge-m3:v1.0           # 内含 FlagEmbedding + FastAPI 包装
  resources:
    limits: {cpu: 4, memory: 8Gi, nvidia.com/gpu: 1}
  ports: [{containerPort: 8080}]
```

API:`POST /embed`,输入文本,返回 1024 维向量。延迟:~5ms/text(GPU)。

---

## 7.7 Reflexion + Skill Drafter Pipeline(v2.0,ADR-011)

### 7.7.1 Reflexion(失败 → prompt 改进)

```python
# Celery 任务
@celery_app.task
def run_reflexion(task_id):
    task = load_task(task_id)
    failure = extract_failure(task)
    
    analysis = call_litellm(
        model="claude-sonnet-4-6",
        messages=[
            {"role": "system", "content": REFLEXION_SYSTEM_PROMPT},
            {"role": "user", "content": build_context(task, failure)}
        ],
        response_format={"type": "json_object"}
    )
    
    save_to_prompt_improvement_candidates(analysis)
    notify_team_via_slack("New prompt improvement candidate")
```

触发:任务失败 / 用户负面反馈 / HITL 拒绝时,主编排发 Celery 任务到 `flywheel_tasks` 队列。

### 7.7.2 Skill Drafter(高满意度 → Skill 草稿)

```python
@celery_app.task
def run_skill_drafter(task_id):
    task = load_task(task_id)
    if task.user_satisfaction < 4:
        return
    
    if not is_novel_workflow(task):
        return
    
    draft_yaml = call_litellm(
        model="claude-sonnet-4-6",
        messages=[
            {"role": "system", "content": SKILL_DRAFTER_PROMPT},
            {"role": "user", "content": build_trace_context(task)}
        ]
    )
    
    if validate_skill_yaml(draft_yaml):
        save_to_skill_drafts(task.user_id, task.id, draft_yaml)
        notify_user_via_ws(task.user_id, "你刚才那个工作流可以发布为 Skill,要试试吗?")
```

### 7.7.3 配额

每个用户每天最多 3 次 Reflexion / 1 次 Skill Drafter(避免连环失败时刷爆成本)。

---

## 18. V1 上线 Checklist

### 18.1 工程基建(本文档)

- [ ] 认证 / 授权(P0)
- [ ] WebSocket 网关(P0)
- [ ] 完整 schema 落地 + 索引建好(**18 张表**,P0)
- [ ] OSS 接入 + 直传协议(P0)
- [ ] Redis Streams 队列联通(P0)
- [ ] LiteLLM Proxy 配好 L1+L2(P0)
- [ ] **MCP Server 集群(7 个)部署 + 健康检查**(P0,v2.0)
- [ ] **Qdrant 向量库 + BGE-M3 自托管**(P0,v2.0)
- [ ] **HITL gate 后端管理器 + 中断 C 回滚算法**(P0,v2.0)
- [ ] **数据飞轮 4 类信号沉淀 + Reflexion / Skill Drafter pipeline**(P0,v2.0)
- [ ] Celery 视频任务(P0)
- [ ] 配额限流落地(P0)
- [ ] Secrets 管理就绪(P0)
- [ ] dev 环境 docker-compose 一键起(P1)
- [ ] staging K8s 部署(P1)
- [ ] Prometheus + Grafana 监控大盘(P1)
- [ ] CI 流水线绿色(含 **MCP 集成测试 + HITL Playwright + 飞轮信号 PR check**,P1)
- [ ] Alembic 迁移就绪(P1)
- [ ] 错误码表 + 中间件统一处理(P1)

### 18.2 业务功能

- [ ] 主编排 **8** 子模块跑通端到端(含 HITL 网关管理器)
- [ ] 4 个 Agent 全部 task_type 至少有 1 个 happy path
- [ ] **短视频 Skill 端到端**(含 3 道 HITL gate)
- [ ] 电商详情图 Skill 端到端
- [ ] 两种模式切换流畅
- [ ] 偏好沉淀(default_mode + 偏好向量)生效
- [ ] 中断 C(回滚)端到端验证
- [ ] 卡片式分享 3 类(群 / 成果 / Agent)
- [ ] 离线继续运行验证
- [ ] 数据飞轮 4 类信号沉淀验证(每 PR CI 检查)

### 18.3 内容与运营

- [ ] BGM 素材库填充(每个 mood ≥ 5 首,共 ≥ 30 首)
- [ ] Skill 样本图填充(澄清形式 3 用)
- [ ] 平台预置 Skill 至少 2 个上架
- [ ] 用户协议 + 隐私政策审核
- [ ] 客服 / 反馈通道

---

## 19. 相关文档

- [产品功能与交互文档](../0_总览/产品功能与交互文档.md) — V1 范围对照
- [主编排 Agent 实现指南](../2_工程实现/主编排 Agent 实现指南.md) — 业务逻辑
- [4 个分任务 Agent 实现指南](../2_工程实现/4 个分任务 Agent 实现指南.md) — 业务逻辑
- [模型路由表](../4_附录/模型路由表.md) — LiteLLM 配置
- [系统 Prompt 全集](../4_附录/系统 Prompt 全集.md) — Prompt 唯一源
- [开放问题与决议](../3_决策记录/开放问题与决议.md) — 架构决策
